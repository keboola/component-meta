{
    "authorization": {
        "oauth_api": {
            "credentials": {
                "token": "token",
                "access_token": "token",
                "#data": "{\"access_token\": \"token\"}"
            }
        }
    },
    "parameters": {
        "api-version": "v23.0"
    }
}
